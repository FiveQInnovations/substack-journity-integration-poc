#!/bin/bash
# Simple deploy script for Cloudflare Pages
# This is a no-op since Pages handles static file serving automatically
# But Cloudflare requires a deploy command, so we provide this minimal script

echo "Deploying static site to Cloudflare Pages..."
echo "Static files will be served automatically by Cloudflare Pages"
exit 0
