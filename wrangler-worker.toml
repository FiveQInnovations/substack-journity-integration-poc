# Cloudflare Worker configuration for Substack proxy
# Deploy this separately from Pages

name = "substack-journity-integration-poc"
main = "worker.js"
compatibility_date = "2024-01-01"

[env.production]
name = "substack-journity-integration-poc"

# Routes (optional - can also use custom domain)
# routes = [
#   { pattern = "substack-proxy.yourdomain.com/*", zone_name = "yourdomain.com" }
# ]
